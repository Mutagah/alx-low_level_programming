#include <stdlib.h>
#include "main.h"
/**
 * _abs - calculates the absolute value of the parameter
 * @n : the integer number to be checked
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
	return (abs(n));
}
