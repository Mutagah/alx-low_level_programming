#include "main.h"
#include <string.h>
/**
 * _strlen - length of a string
 * @s: pointer to the parameter of type char
 * Return: length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
